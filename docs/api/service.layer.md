# form.service.ts

```
createForm()
    ├── unique slug
    └── default values

getForm()
    └── user ownership

getForms()
    └── user's forms only

updateForm()
    ├── user ownership
    └── unique slug

deleteForm()
    └── user ownership
```
